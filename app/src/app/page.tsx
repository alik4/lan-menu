export default function Home() {
  return (
    <main style={{ padding: 40, textAlign: "center", fontFamily: "sans-serif" }}>
      <h1>QR Menu System</h1>
      <p>
        This page has no content on its own — each outlet has its own QR code pointing to
        its menu, e.g. <code>/menu/dweik</code>.
      </p>
    </main>
  );
}
